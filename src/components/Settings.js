import React from 'react';
import { Box, Typography } from '@mui/material';

const Settings = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Settings</Typography>
      <Typography>Settings features will be implemented soon.</Typography>
    </Box>
  );
};

export default Settings;
